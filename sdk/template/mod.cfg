[mod]
name = "My Mod"
version = "1.0.0"
author = "Your Name"
description = "Short description of what the mod does."
game_version = "2.8.7"
load_order = 0

[dependencies]
; other_mod_id = true

[config]
; my_option = true         # Default values — available via config dict / get_cfg()
; my_value = 1.0           # Значения по умолчанию — доступны через config / get_cfg()
